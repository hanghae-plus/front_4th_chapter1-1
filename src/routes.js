const HOME = "/";
const PROFILE = "/profile";
const LOGIN = "/login";
const LOGOUT = "/logout";

const routes = {
  home: HOME,
  profile: PROFILE,
  login: LOGIN,
  logout: LOGOUT,
};

export default routes;
